#include "main.h"
/**
 * print_alphabet - classify numbers
 * Return: Always 0
 */

void print_alphabet(void)
{
	char i, newL;

	newL = '\n';
	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}

	_putchar(newL);
}
