#include "main.h"

/**
 * print_rev - print string
 * @str: character
 * Return: void
 */

void print_rev(char *str)
{
	int i, len;
	char *cpy = str;

	len = 0;
	while (*str != '\0')
	{
		len++;
		str++;
	}

	for (i = len - 1; i >= 0; i--)
	{
		_putchar(*(cpy + i));
	}
	_putchar('\n');
}
