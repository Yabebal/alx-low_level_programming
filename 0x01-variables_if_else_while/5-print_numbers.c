#include<stdio.h>
#include<string.h>
/**
 * main - classify numbers
 * Return: Always 0
 */

int main(void)
{
	char newL, i;

	newL = '\n';
	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}

	putchar(newL);

	return (0);
}
