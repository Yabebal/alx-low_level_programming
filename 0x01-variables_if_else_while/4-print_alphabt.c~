#include<stdio.h>
#include<string.h>
/**
 * main - classify numbers
 * Return: Always 0
 */

int main(void)
{
	char i, newL;

	newL = '\n';
	for (i = 'a'; i <= 'z'; i++)
	{
		if (i != 'q' || i != 'e')
			putchar(i);
	}

	putchar(newL);

	return (0);
}
